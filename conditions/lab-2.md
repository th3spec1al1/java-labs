# Лабораторная работа 2

## Предисловие 🤓
Во второй лабораторной работе вы не будете существенно расширять бизнес-функциональность системы,  
реализованной в первой лабораторной работе.

Основная цель — **адаптировать уже написанную доменную и бизнес-логику под современные backend-технологии**,  
используемые в промышленной разработке.

В рамках работы вы познакомитесь с экосистемой **Spring Boot**,  
научитесь работать с **реляционной базой данных PostgreSQL**,  
реализуете **REST API**, миграции БД и **интеграционные тесты**.

По итогам лабораторной работы система должна превратиться в полноценное backend-приложению с БД и HTTP-интерфейсом.

---

## Цель лабораторной работы
- освоить базовые возможности Spring Boot;
- научиться работать с PostgreSQL через Spring Data JPA;
- познакомиться с миграциями базы данных;
- освоить принципы построения REST API;
- получить практический опыт написания интеграционных тестов.

---

## Задание
Необходимо:
- интегрировать существующий проект с Spring Boot;
- реализовать подключение и работу с реляционной БД;
- реализовать REST API для доступа к бизнес-функциональности системы;
- обеспечить корректную работу миграций и интеграционных тестов.

---

## Описание системы

### Общая логика
Бизнес-логика системы **остается прежней** и должна соответствовать требованиям,  
описанным в лабораторной работе №1.

Все сущности, сценарии использования, проверки совместимости и предметные исключения  
должны быть сохранены без упрощений.

---

## Архитектурные требования

Приложение может быть реализовано как в трёхслойной архитектуре, так и в любой другой многослойной на ваш выбор.

1. **Слой представления (Integration Layer)**:
	- REST API;
	- Spring Web (Controllers);
	- использование DTO;
	- отсутствие прямой работы с JPA-сущностями.

2. **Бизнес-слой (Service Layer)**:
	- вся доменная логика;
	- валидации;
	- предметные исключения.

3. **Слой доступа к данным (Persistence Layer)**:
	- Spring Data JPA;
	- репозитории;
	- работа с PostgreSQL.

---

## Требования к работе с базой данных

### Подключение БД
- Использовать **PostgreSQL**.
- База данных должна подниматься в Docker-контейнере.
- Подключение к БД осуществляется через Spring Data JPA.

### Миграции
- Для управления схемой БД использовать **Liquibase**.
- Миграции должны:
	- создавать таблицы в соответствии с ранее спроектированной ER-диаграммой;
	- наполнять БД начальными (seed) данными.

---

## JPA-сущности

### Базовая сущность
Все JPA-сущности должны наследоваться от базовой сущности, содержащей следующие поля:

- `UUID id`;
- `Instant createdAt`;
- `Instant updatedAt`;
- `boolean removed` — для реализации **soft delete**.

---

## Репозитории

- Репозитории необходимо реализовать с помощью **JPA-репозиториев Spring Data**.
- Запрещено использовать стандартные коллекции в качестве хранилища данных.

---

## REST API

### Общие требования
- Интеграционный слой приложения должен быть реализован в виде **REST API**.
- Запрещено передавать JPA-сущности наружу.
- Взаимодействие осуществляется **исключительно через DTO**.
- Для маппинга DTO ↔ Entity необходимо использовать библиотеки: ModelMapper или MapStruct.

---

## Фильтрация данных

### Фильтрация комплектаций
В методе получения списка базовых комплектаций автомобилей необходимо реализовать фильтрацию по:
- марке автомобиля;
- конкретным комплектующим (узлам конфигурации).

Фильтрация должна:
- выполняться **на уровне запроса к БД**;
- быть реализована через **Spring Data Specifications**.

---

## Документация API

- Необходимо подключить автоматическую генерацию Swagger-документации.
- Документация должна генерироваться на основе REST-контроллеров.
- По адресу `/swagger-ui.html` должна быть доступна интерактивная страница API.

---

## Тестирование

### Интеграционные тесты
Необходимо реализовать интеграционные тесты, проверяющие:
- корректность выполнения миграций БД;
- работу JPA-репозиториев;
- корректность REST API.

### Инструменты
- Для интеграционных тестов БД использовать **Testcontainers**.
- PostgreSQL должен подниматься в Docker-контейнере автоматически во время тестов.

---

## Нефункциональные требования
- PostgreSQL запускается через Docker.
- Работа с БД осуществляется через Spring Data JPA.
- Миграции БД выполняются через Liquibase.
- Интеграционные тесты БД реализованы с использованием Testcontainers.
- Система сборки: Gradle.

---

## Результат выполнения лабораторной работы
В результате выполнения лабораторной работы система должна:
- работать как Spring Boot приложение;
- хранить данные в PostgreSQL;
- предоставлять REST API;
- иметь документированное API;
- успешно проходить интеграционные тесты.

Бизнес-логика и доменные правила, реализованные в лабораторной работе №1,  
должны быть полностью сохранены.
